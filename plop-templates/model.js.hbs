// Import core modules and dependencies
const mongoose = require('mongoose');

/**
 * @name {{titleCase}}Schema
 * @description Mongoose schema for the {{titleCase name}} model.
 * @typedef {Object} {{titleCase name}}
 * @constant
 * @property {string} [property name] - [property description].
 */
const {{camelCase name}}Schema = new mongoose.Schema(
 {},
 {
  timestamps: true,
 },
);

/**
 * Add your custom schema methods here (remove if not needed)
 */

/**
 * @name toJSON
 * @description Customizes the JSON representation of the {{titleCase name}} model documents.
 * @function
 * @param {Document} document - The Mongoose document being transformed.
 * @param {Object} returnedObject - The plain JS object document representation.
 */
{{camelCase name}}Schema.set('toJSON', {
  transform: (document, returnedObject) => {
    returnedObject.id = returnedObject._id.toString();
    delete returnedObject._id;
    delete returnedObject.__v;
    // delete any other properties here
    delete returnedObject.createdAt;
    delete returnedObject.updated;
  },
});

// Exporting the {{titleCase name}} model
module.exports = mongoose.model('{{titleCase name}}', {{camelCase name}}Schema);
